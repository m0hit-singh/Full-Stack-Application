const HOST = "localhost";
const USERNAME = "root";
const PASSWORD = "root";
const DATABASE_NAME = "test";

const dbConfig = {
  host: HOST,
  username: USERNAME,
  password: PASSWORD,
  database_name: DATABASE_NAME,
};
module.exports = dbConfig;
